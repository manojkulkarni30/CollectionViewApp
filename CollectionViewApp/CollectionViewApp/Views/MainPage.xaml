<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CollectionViewApp"
             x:Class="CollectionViewApp.Views.MainPage"
             Title="Collection View">

    <StackLayout>
        <Button Text="Basic" Command="{Binding BasicCommand}"></Button>

        <Button Text="Carousel View" Command="{Binding CarouselCommand}"></Button>

        <Button Text="Grid View" Command="{Binding GridCommand}"></Button>

        <Button Text="Empty View" Command="{Binding EmptyCommand}"></Button>
    </StackLayout>

</ContentPage>
